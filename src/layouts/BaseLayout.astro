---
import '../styles/global.css';

export interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonical?: string;
}

const {
  title = 'Juliana Capucci — Psicoterapia Online',
  description = 'Juliana Capucci (CRP 06/209216) é Psicóloga Clínica. Oferece Psicoterapia Online para adolescentes e adultos através da Terapia Cognitivo-Comportamental (TCC) focada em ansiedade, depressão e bem-estar.',
  image = '/img/profile.jpg',
  canonical,
} = Astro.props;

const canonicalUrl = canonical || Astro.url.href;
const siteUrl = Astro.site?.toString().replace(/\/$/, '') || '';
const absoluteImage = image.startsWith('http') ? image : `${siteUrl}${image}`;
---

<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>

  <!-- Agentic e Tradicional SEO -->
  <meta name="description" content={description}>
  <!-- Keywords em formato NLU/Intent para IA contextualizar o conteúdo como respostas à pesquisas -->
  <meta name="keywords" content="Psicóloga Clínica, Psicoterapia Online para ansiedade e depressão, Terapia Cognitivo-Comportamental TCC online, Psicóloga para adolescentes e adultos, Juliana Capucci CRP 06/209216">
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1">
  <link rel="canonical" href={canonicalUrl}>

  <!-- Open Graph -->
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:type" content="website">
  <meta property="og:image" content={absoluteImage}>
  <meta property="og:url" content={canonicalUrl}>
  <meta property="og:locale" content="pt_BR">
  <meta property="og:site_name" content="Juliana Capucci — Psicoterapia Online">

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  <meta name="twitter:image" content={absoluteImage}>

  <!-- Theme -->
  <meta name="theme-color" content="#C4908A">

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">

  <!-- Fonts (preload) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&family=DM+Sans:wght@300;400;500&display=swap"
    rel="stylesheet">



  <!-- JSON-LD Structured Data enriquecido para IAs entenderem contexto clínico -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": ["MedicalClinic", "MedicalOrganization"],
        "@id": `${siteUrl}/#organization`,
        "name": "Juliana Capucci — Psicoterapia Online",
        "description": "Serviço de Psicoterapia online especializado em Terapia Cognitivo-Comportamental (TCC) para adolescentes e adultos. Tratamento de ansiedade e depressão.",
        "url": siteUrl,
        "telephone": "+5511993051221",
        "areaServed": "BR",
        "medicalSpecialty": "Psychiatric",
        "availableService": {
          "@type": "TherapeuticProcedure",
          "name": "Terapia Cognitivo-Comportamental (TCC)",
          "description": "Intervenção psicoterapêutica focada na interação entre pensamento, emoção e comportamento para tratamento de transtornos de saúde mental e promoção de bem-estar."
        },
        "image": absoluteImage,
        "sameAs": [
          "https://instagram.com/psijulianacapucci"
        ]
      },
      {
        "@type": "Person",
        "@id": `${siteUrl}/#person`,
        "name": "Juliana Capucci",
        "jobTitle": "Psicóloga Clínica",
        "description": "Juliana Capucci (CRP 06/209216) é psicóloga clínica formada desde 2019 e pós-graduada em Avaliação Psicológica pelo IPOG. Oferece atendimentos online.",
        "url": siteUrl,
        "image": absoluteImage,
        "knowsAbout": [
          "Terapia Cognitivo-Comportamental",
          "Psicologia Clínica",
          "Psicoterapia Online",
          "Tratamento de Ansiedade",
          "Regulação Emocional"
        ],
        "sameAs": [
          "https://instagram.com/psijulianacapucci"
        ],
        "worksFor": {
          "@id": `${siteUrl}/#organization`
        }
      }
    ]
  })} />

  <!-- Web Manifest -->
  <link rel="manifest" href="/site.webmanifest">
</head>

<body>
  <slot />

  <script>
    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    const obs = new IntersectionObserver((entries) => {
      entries.forEach((entry, i) => {
        if (entry.isIntersecting) {
          setTimeout(() => entry.target.classList.add('visible'), i * 80);
        }
      });
    }, { threshold: 0.1 });
    reveals.forEach(el => obs.observe(el));

    // Smooth nav
    document.querySelectorAll('a[href^="#"]').forEach(a => {
      a.addEventListener('click', e => {
        const target = document.querySelector(a.getAttribute('href')!);
        if (target) {
          e.preventDefault();
          target.scrollIntoView({ behavior: 'smooth' });
          const navLinksEl = document.getElementById('navLinks');
          const menuToggleEl = document.getElementById('menuToggle');
          navLinksEl?.classList.remove('open');
          menuToggleEl?.classList.remove('active');
        }
      });
    });

    // Mobile menu toggle
    const menuToggle = document.getElementById('menuToggle');
    const navLinks = document.getElementById('navLinks');
    menuToggle?.addEventListener('click', () => {
      menuToggle.classList.toggle('active');
      navLinks?.classList.toggle('open');
    });

    // Nav scroll condensation
    const mainNav = document.getElementById('mainNav');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 60) {
        mainNav?.classList.add('nav-scrolled');
      } else {
        mainNav?.classList.remove('nav-scrolled');
      }
    }, { passive: true });
  </script>
</body>

</html>
